# MDP (Markov decision procession )

state = "abc"
while state != "home":
  action  = "walk"
  if action == "walk":
    state = "home"
    reward = 10
  else:
    state = "Road"
    reward = -1
  print(state, reward)
print("Reached Home")
